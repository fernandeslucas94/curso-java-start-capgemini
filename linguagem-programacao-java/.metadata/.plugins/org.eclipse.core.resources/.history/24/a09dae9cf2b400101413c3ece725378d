/** 
 * Nesta seção de estudos, iremos conhecer os conceitos acerca de vetores e matrizes no Java.
 * 
 * O que é vetor?
 * 
 * Vetor, é o que também é conhecido como array. No caso do do vetor, ele é um array unidimencional. Ou seja,
 * ele irá armazenar somente um tipo de dado.
 * 
 * O que é matriz?
 * 
 * Matriz, que também é conhecido como array, mas neste caso, array multidimencional, diferente do vetor,
 * poderá armazenar mais de um tipo de dado e múltiplos dados. Podendo também, armazenar vetores.
 * 
 * 
 * 
 * Os arrays, ou vetores, são estruturas de dados que possibilitam o armazenamento de informações para que possam ser utilizadas posteriormente.
 * À título de exemplo, pode-se fazer uma analogia com uma caixa, onde desejamos guardar livros. Poderemos,
 * guardar diversos livros em uma caixa para que possamos transportá-los, e posteriormente, poderemos acessá-los para que possamos consultar algo.
 * 
 * Com os arrays, ou vetores, é da mesma forma. Podemos armazenar dados, como um coleção, e em outro momento da aplicação,
 * teremos a possibilidade de manipular os dados e utilizá-los para algum fim.
 * 
 * Diferente de outras linguagens de programação, no Java, temos algumas particularidades. Deveremos definir qual o tipos dos dados que serão armazenados,
 * colocando os sinais de colchetes, posteriormente, após o sinal de atribuição de valor, devemos inserir a palavra reservada new,
 * que irá explicitar que está sendo "construido" um novo array, e após isto, deve-se, novamente, informar o tipo dos dados, e entre colchetes,
 * deve-se informar o número de ítens que serão armazenados.
 * 
 * Porém, além da maneira que explicamos acima, existem outras formas de criarmos, ou declararmos um array no Java.
 * 
 * Vejamos abaixo alguns exemplos.
 * */

package conceitosMatriz;

import javax.swing.JOptionPane;

public class MatrizPrincipal {

	public static void main(String[] args) {
		
		/** 
		 * Vejamos abaixo alguns exemplos de declaração de arrays no Java.
		 * */
		
		// No exemplo abaixo iremos declarar um array da forma tradicional, como explicado acima.
		/** 
		 * Neste exemplo, declaramos um array de números e definimos que irá ter quatro posições. Logo após,
		 * necessitamos atribuir de um em um os valores para cada posição do array. Lembrando que no Java,
		 * a contagem de posições, ou indices, se inicia em zero.
		 * */
		int[] numeros = new int[4];
		numeros[0] = 1;
		numeros[1] = 2;
		numeros[2] = 3;
		numeros[3] = 4;
		
		// Também podemos declarar arrays de um mesmo tipo de dado, em sequẽncia, conforme fazemos ao declarar variáveis.
		int[] numeros1, numeros2;
		
		
		/** 
		 * No exemplo abaixo iremos verificar outra forma de declararmos array. Neste exemplo, 
		 * iremos declarar o array já especificando os dados que serão armazenados. Diferente dos exemplos acima,
		 * para declarar os valores, ou dados, ao invés de utilizarmos os colchetes, iremos utilizar chaves.
		 * */
		
		String[] nomes = {"José", "Maria", "Jesus"};
		
		
		
		/** 
		 * Agora iremos conhecer melhor os conceitos sobre matriz.
		 * 
		 * Q que é matriz? 
		 * 
		 * Como informado anteriormente, matriz é uma estrutura de dados a qual é utilizada para armazenar vetores.
		 * 
		 * De forma superficial, podemos dizer que a matriz é um vetor de vetores. Sendo assim,
		 * podemos, quando necessário, armazenar dados em vetores diferentes, que por sua vez,
		 * irão ser armazenados em uma matriz.
		 * 
		 * Semelhante ao vetor, na matriz, ao declararmos que iremos utilizar um vetor como valor para a variável,
		 * devemos adicionar os colchetes após o tipo dos dados. Porém, neste caso, ao contrário de somente um par de colchetes,
		 * iremos utilizar dois. Desta forma, iremos estar dizendo que será um vetor de vetores.
		 * 
		 * Veremos abaixo alguns exemplos sobre matriz.
		 * */
		
		// No exemplo abaixo, iremos armazenar e calcular as notas e a media de três alunos.
		
		String[] nomeAlunos = {"José", "Maria", "João"};
		
		// Matriz que irá conter as notas dos alunos.
		float[][] notas = new float[3][4];
		
		// Irá armazenar a soma das notas do aluno e a média das notas.
		float somaNotas, mediaAluno;
		
		// Irá armazenar o status do aluno para ser apresentado posteriormente.
		String statusAluno;
		
		// Abaixo iremos realizar as operações.
		
		// O laço de repetição abaixo, matriz, irá ser utilizado para associar os alunos às notas.
		for(int i = 0; i < 3; i++) {
			System.out.println("Aluno" + nomeAlunos[i]);
			
			somaNotas = 0;
			mediaAluno = 0;
			
			for(int j = 0; j < 4; j++) {
				notas[i][j] = Float.parseFloat(JOptionPane.showInputDialog("Informe a nota nº" + j +1 + " do aluno" + i));
				somaNotas += notas[i][j];
			}
			
			mediaAluno = somaNotas / 4;
			
			if(mediaAluno < 5.5) {
				System.out.println("O aluno está reprovado");
			} 
			
			if(mediaAluno >= 5.5 && mediaAluno < 6.5) {
				System.out.println("O aluno está de recuperação");
			}
		}
	}
}
